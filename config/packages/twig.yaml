twig:
    default_path: '%kernel.project_dir%/application/views/templates'
    globals:
        app_name: '%app.name%'
        app_version: '%app.version%'
    form_themes: ['bootstrap_5_layout.html.twig']

when@test:
    twig:
        strict_variables: true
